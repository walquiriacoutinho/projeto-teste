name: unitario

on:
push:
branches: ["test_unitarios"]
pull_request:
branches: ["test_unitarios"]

jobs:
test:
runs-on: ubuntu-latest

steps:
  - name: Checkout do código
    uses: actions/checkout@v4

  - name: Configurar Python
    uses: actions/setup-python@v5
    with:
      python-version: "3.11"

  - name: Instalar dependências
    run: |
      python -m pip install --upgrade pip
      pip install pytest

  - name: Rodar testes
    run: pytest


